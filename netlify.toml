[build]
  command = "npm run build"
  publish = "dist"

# Clean URL: /builder → /builder.html
[[redirects]]
  from = "/builder"
  to   = "/builder.html"
  status = 200

# SPA fallback: serve index for all non-file routes
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
